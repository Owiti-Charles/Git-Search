import { GitdirectiveDirective } from './gitdirective.directive';

describe('GitdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new GitdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
